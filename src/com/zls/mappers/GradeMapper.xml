<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zls.mappers.GradeMapper">
	
	<resultMap type="Grade" id="gradeResult">
		<id property="id" column="id" />
		<result property="gradeName" column="gradeName"/>
		<collection property="studentList" column="id" select="com.zls.mappers.StudentMapper.findWithGrade"></collection>
	</resultMap>

	<select id="findById" parameterType="Integer" resultMap="gradeResult">
		select * from t_grade where id=#{id}
	</select>
	
</mapper> 